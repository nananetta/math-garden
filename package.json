{
  "name": "math-garden",
  "version": "1.0.0",
  "private": true,
  "description": "A static early-years math quiz deployable to Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
